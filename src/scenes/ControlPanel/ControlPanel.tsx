import React from "react"
import { observer } from "mobx-react-lite"

import classes from "./ControlPanel.module.css"

interface ControlPanelProps {
    //
}

export const ControlPanel = observer(({}: ControlPanelProps) => {
    return (
        <>
            {/**/}
            {/**/}
        </>
    )
})
